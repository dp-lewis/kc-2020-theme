<section class="p-8 pb-0">
	<h1 class="text-2xl text-center mb-4">{{ title }}</h1>
	<div class="flex overflow-x-auto overflow-y-hidden scrolling-touch scroll-snap-both-mandatory">
		{% for post in posts %}
			<div class="w-2/3 md:w-1/5 lg:w-1/6 flex-grow-0 flex-shrink-0 pr-6"> 
			{% include ['tease.twig'] %}
			</div>
		{% endfor %}
	</div>
</section>
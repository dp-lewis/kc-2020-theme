{# 

CSS Classes that are computed or used via CMS. They are listed here so PurgeCSS sees them
md:w-1/2
md:w-1/3
md:w-1/4
md:w-1/5
md:w-1/6
bg-kombigrey

#}
<section class="p-10 {{ cssclasses }}">
	<h1 class="text-xl text-center leading-none mb-6">{{ title }}</h1>
	{% block content %}
	<div class="flex overflow-x-auto overflow-y-hidden scrolling-touch scroll-snap-both-mandatory">
		{% for post in posts %}
			<div class="flex w-3/4 md:w-1/{{ min([totalPosts, 5]) }} flex-grow-0 flex-shrink-0 pr-6"> 
			{% include ['listview-' ~ post.post_type ~ '.twig', 'listview.twig'] %}
			</div>
		{% endfor %}
	</div>
	{% endblock %}
</section>